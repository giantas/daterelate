from setuptools import setup

setup(name='daterelate',
      version='0.0.1',
      description='Relate dates to each other',
      url='http://github.com/giantas/daterelate',
      author='Aswa Paul',
      license='MIT',
      packages=['daterelate'],
      install_requires=[
          'testfixtures',
      ],
      zip_safe=False)